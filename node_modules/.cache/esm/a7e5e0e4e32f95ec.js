let createStore;_e32‍.x([["increment",()=>increment],["decrement",()=>decrement],["default",()=>_e32‍.o]]);_e32‍.w("redux",[["createStore",["createStore"],function(v){createStore=v}]]);

//ACTION TYPES
const INCREMENT = "INCREMENT";
const DECREMENT = "DECREMENT";

//ACTION CREATORS
       const increment = (value = 1) => {
  //javascript giriyor
  return {
    //obje
    type: INCREMENT,
    payload: value,
  };
};
       const decrement = (value = 1) => ({
  type: DECREMENT,
  payload: value,
});

const counterReducer = (counter = 0, action) => {
  switch (action.type) {
    case INCREMENT:
      return counter + action.payload;
    case DECREMENT:
      return counter - action.payload;
    default:
      return counter;
  }
};

_e32‍.d(counterReducer);
